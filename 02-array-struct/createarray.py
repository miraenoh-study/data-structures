# Create a list
list = [1, 2, 3, 4]

# Append an element
list.append(5)

# Get an element with index
first_el = list[0]

# Create a list with n zeros
n = 5
list = [0 for i in range(n)]

# Create a 2D list with n * n zeros
list = [[0 for j in range(n)] for i in range(n)]

# Get an element with an index from a 2D list
first_el = list[0][0]
